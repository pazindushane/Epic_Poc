import { Injectable } from '@angular/core';
import {userDTO} from "../dto/driverDTO";
import {Observable} from "rxjs";

@Injectable({
  providedIn: 'root'
})
export class UsersubService {

  constructor() { }

  // saveUser(driverDTO: userDTO): Observable<any> {
  //
  // }
}
